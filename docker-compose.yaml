version: '3'
services:
  mongodb:
    image: mongo
    ports:
      - 27017:27017
    volumes:
      - mongodb_data:/data/db

  vision:
    build:
      context: .
      dockerfile: vision.Dockerfile
    command: ["bin/vision"]
    volumes:
      - /tmp:/tmp
    ports:
      - 8080:8080
    depends_on:
      - mongodb

volumes:
  mongodb_data:
